

// the purpose here is to bring the Sequelize instance ('db ) togther 
//with the models being built in this file 

const db = require('./database');


// add associations here 

module.exports = {
  db,
}